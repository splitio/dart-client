name: split_dart_sdk_root
description: Split Dart SDK monorepo root (dev-only).
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dev_dependencies:
  melos: ^4.0.0
  test: ^1.24.0
  lints: ^3.0.0
