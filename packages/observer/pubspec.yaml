name: observer
description: Generic pub/sub primitive (dependency-free, no I/O). Decouples internal emitters (factory, sync, streaming, HTTP, auth, recorders) from consumers.

# TODO: remove once ready to publish to pub.dev
publish_to: none
version: 0.0.1

environment:
  sdk: ^3.0.0

dev_dependencies:
  test: ^1.24.0
  lints: ^3.0.0
