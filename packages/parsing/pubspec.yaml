name: parsing
description: Wire DTO → ParsedSplit, wire rules → TargetingRule, unsupported-matcher detection.

# TODO: remove once ready to publish to pub.dev
publish_to: none
version: 0.0.1

environment:
  sdk: ^3.0.0

dependencies:
  models:
    path: ../models
  json_annotation: ^4.8.0

dev_dependencies:
  test: ^1.24.0
  lints: ^3.0.0
  build_runner: ^2.4.0
  json_serializable: ^6.7.0
