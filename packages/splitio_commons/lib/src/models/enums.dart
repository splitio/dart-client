enum ImpressionsMode { optimized, debug, none }

enum ConsentStatus { granted, declined, unknown }

enum DataType { number, datetime }

enum ConditionType { rollout, whitelist }

enum CombinerEnum { and }
