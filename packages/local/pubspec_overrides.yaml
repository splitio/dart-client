# melos_managed_dependency_overrides: engine,models,parsing,storage
dependency_overrides:
  engine:
    path: ../engine
  models:
    path: ../models
  parsing:
    path: ../parsing
  storage:
    path: ../storage
