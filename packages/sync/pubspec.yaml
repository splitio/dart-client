name: sync
description: Inbound freshness coordinator—SyncManager, FeedSynchronizer, streaming FSM, on-demand fetch coordinator, sync delays. Pure FSM (StreamingPolicy) separated from effect runtime. Coordinates rules + memberships convergence.
version: 0.0.1

environment:
  sdk: ^3.0.0

dependencies:
  models:
    path: ../models
  storage:
    path: ../storage
  parsing:
    path: ../parsing
  auth:
    path: ../auth

dev_dependencies:
  test: ^1.24.0
  lints: ^3.0.0
