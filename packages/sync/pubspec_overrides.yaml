# melos_managed_dependency_overrides: auth,models,parsing,storage
dependency_overrides:
  auth:
    path: ../auth
  models:
    path: ../models
  parsing:
    path: ../parsing
  storage:
    path: ../storage
